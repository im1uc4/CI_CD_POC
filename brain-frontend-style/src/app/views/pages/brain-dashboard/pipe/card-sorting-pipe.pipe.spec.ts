import { CardSortingPipe } from './card-sorting-pipe.pipe';

describe('CardSortingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CardSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
