import { DashboardElement } from './dashboard-element';

describe('DashboardElement', () => {
  it('should create an instance', () => {
    expect(new DashboardElement()).toBeTruthy();
  });
});
