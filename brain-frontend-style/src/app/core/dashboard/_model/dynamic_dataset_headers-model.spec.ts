import { DynamicHeader } from './dynamic_dataset_headers-model';

describe('DynamicHeader', () => {
  it('should create an instance', () => {
    expect(new DynamicHeader()).toBeTruthy();
  });
});
